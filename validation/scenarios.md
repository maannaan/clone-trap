# Scenarios

| ID | Intent | Expected shape |
| --- | --- | --- |
| A | Clean stdlib repo (change-neighbor) | `ready`, zero traps |
| B | Mostly portable package (merge-memory) | `ready` or documented assumptions only |
| C | Mixed app with env examples (Mend) | Assumptions for documented secrets; no env flood |
| D | Compose + extra demo tooling (Ripple) | Postgres declared, not a trap; extra tools may appear |
| E | Compose Redis/Postgres (Deplot) | Declared services silent; extra CLI/tokens possible |
| F | Submodule + PowerShell + missing data (continuum) | Submodule, OS-specific bootstrap, missing artifacts |
